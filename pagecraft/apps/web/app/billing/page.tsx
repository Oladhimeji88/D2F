export default function BillingPage() {
  return (
    <section className="panel p-8">
      <p className="text-xs font-semibold uppercase tracking-[0.18em] text-blue-600">Billing</p>
      <h2 className="mt-3 text-3xl font-semibold text-ink">Billing placeholder</h2>
      <p className="mt-4 max-w-2xl text-sm leading-6 text-slate-500">
        Future production work should add plans, usage-based metering, invoices, and seat management. For the MVP, this page exists so the navigation and information architecture are coherent.
      </p>
    </section>
  );
}
