export default function SettingsPage() {
  return (
    <section className="panel p-8">
      <p className="text-xs font-semibold uppercase tracking-[0.18em] text-blue-600">Settings</p>
      <h2 className="mt-3 text-3xl font-semibold text-ink">Workspace settings placeholder</h2>
      <p className="mt-4 max-w-2xl text-sm leading-6 text-slate-500">
        Future work should move API keys, storage config, organization preferences, and role-based access controls here.
      </p>
    </section>
  );
}
