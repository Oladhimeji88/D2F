{
  "name": "@pagecraft/plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "clean": "rimraf dist",
    "dev": "node ./scripts/build.mjs --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"process.exit(0)\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@pagecraft/figma-export": "workspace:*",
    "@pagecraft/shared-types": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "esbuild": "^0.25.1"
  }
}
